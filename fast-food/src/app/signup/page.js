//this page is used as a confirmation to tell the user that the signup was successfull. Since the signup form posts the request to "signup"

export default function signup(){
    return(
        <main>
        <h1>Sign up succesfull!</h1>
        <a href="/Login">Log in</a>
        </main>
    )
}